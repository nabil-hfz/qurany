import { BaseFilter } from "./base.filter";

/**
 * Interface for filtering Reciter data.
 * Extends the BaseFilter interface for pagination and additional filtering options.
 */
export interface ReciterFilter extends BaseFilter {

}
